interface:
  display_name: "SQL Query Performance"
  short_description: "Review SQL query text for performance risks"
  default_prompt: "Statically review this project's SQL queries and suggest safe performance improvements without executing them."
